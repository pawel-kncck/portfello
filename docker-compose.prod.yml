services:
  app:
    image: ghcr.io/${GITHUB_REPOSITORY:-pawel-kncck/portfello}:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      AUTH_SECRET: ${AUTH_SECRET}
      AUTH_URL: ${AUTH_URL}
